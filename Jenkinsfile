pipeline {
       agent any
           stages {
             stage('build') {
                 steps {
                      echo 'build project'
                      sh "sudo npm install"
                
               }
        }
    }

}

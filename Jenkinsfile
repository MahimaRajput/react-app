pipeline {
       environment {
                  PATH = "C:\\Program Files\\Git\\usr\\bin
    stages {
        stage('build') {
            steps {
                echo 'build project'
                sh 'npm install'
                
            }
        }
    }
}
}
